#include "main.h"

/**
 * _isdigit - check for upper case letter
 * @c : character to check the case
 * Return:0 or 1
 */

int _isdigit(int c)
{
if (c >= '0' && c <= '9')
{
return (1);
}
else
{
return (0);
}
}
